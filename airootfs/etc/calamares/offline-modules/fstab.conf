---
# Generate /etc/fstab from partitions selected and mounted by Calamares.
# Rootfs payload deliberately does not contain /etc/fstab.
mountOptions:
  default: defaults
  ext4: defaults,noatime
  btrfs: defaults,noatime,compress=zstd
  xfs: defaults,noatime
  vfat: umask=0077
